services:
  - type: web
    name: svs-shop-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: svs-beauty-db
          property: connectionString
